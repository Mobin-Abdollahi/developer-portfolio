export const metadata = {
  title: "Project Details | Mobin's Portfolio",
  description: "Detailed view of my projects and technical work",
};

export default function ProjectsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
